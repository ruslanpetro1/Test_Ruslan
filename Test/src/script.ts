// Ваш TypeScript код
document.getElementById("teamForm")?.addEventListener("submit", (e) => {
    e.preventDefault();

    // Додатковий код обробки форми
    console.log("Form submitted!");
});
